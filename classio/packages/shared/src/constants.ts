export const DEFAULT_API_PORT = 3000;
export const DEFAULT_RUNTIME_PORT = 3001;

export const ROUTES = {
    DEPLOY: '/deploy',
    CODE: '/code',
    DEPLOYMENT: '/deployment',
    INVALIDATE: '/invalidate',
} as const;
