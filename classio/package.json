{
  "name": "classio",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:api": "bun --watch packages/api-server/src/index.ts",
    "dev:runtime": "bun --watch packages/runtime-worker/src/index.ts",
    "dev": "bun run dev:api & bun run dev:runtime"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
